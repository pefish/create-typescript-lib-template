{
  "name": "@pefish/template",
  "version": "0.0.1",
  "description": "template_description",
  "keywords": [
    "common",
    "js",
    "tool",
    "ether"
  ],
  "homepage": "https://github.com/pefish/template",
  "bugs": "https://github.com/pefish/template/issues",
  "license": "MIT",
  "author": "pefish",
  "types": "./types/index.d.ts",
  "main": "./lib/index.js",
  "engines": {
    "node": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pefish/template"
  },
  "scripts": {
    "build": "rm -rf lib && rm -rf types && tsc",
    "test": "mocha --timeout 15000 --require ts-node/register src/**/*.test.ts",
    "t": "mocha --timeout 15000 --require ts-node/register",
    "prepublish": "yarn build"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.7",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
